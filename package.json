{
  "name": "@huhuang03/th_comm",
  "version": "0.0.2-rc1",
  "description": "js common utils",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "prepublish": "yarn run build",
    "s_publish": "npm publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
