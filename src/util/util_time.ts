/**
 * return the str time passed
 * 刚刚 < 1分
 * xx分前 < 1小时
 * xx小时前 < 24小时
 * x月x日 this year
 * x年x月x日 not this year
 * @param src_time the need calc time
 */
function time_passed(src_time: Date): string {
  return ""
}
